Flask==3.0.0
flask-cors==4.0.0
Werkzeug==3.0.1
gunicorn==23.0.0  # upgraded to satisfy buildpack (functions-framework) requirement >=22
requests==2.31.0

# Scientific libraries for exoplanet analysis
numpy==1.26.4
pandas==2.2.2
astropy==6.1.2

# Optional (can remove if unused)
astroquery==0.4.7

# Async / file handling (keep minimal)
aiofiles==23.2.1

# Env utilities (optional if .env not needed)
python-dotenv==1.0.1
